# AWS SDK uses the standard credential provider chain (for example, AWS SSO,
# a local AWS profile, or an IAM role). Never put credentials in this file.
AWS_REGION=us-east-1
S3_BUCKET_NAME=csv-insight-data-ranganath-2026
# Optional: selects a locally configured AWS SSO/profile identity.
AWS_PROFILE=csv-insight
